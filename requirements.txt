# Core dependencies - Required
PyQt5>=5.15.0
Flask>=2.0.0
Werkzeug>=2.0.0

# Optional dependencies - Install only if needed
# For Jupyter Notebook support: pip install nbformat>=5.0
# For PDF support: pip install PyPDF2>=2.0.0

